{
  "name": "automatic-mailing-cat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs",
    "preview:fixture": "tsx src/cli/previewFixture.ts fixtures/adoptions-with-errors.csv",
    "push": "npm run build && clasp push"
  },
  "devDependencies": {
    "@google/clasp": "^3.0.6",
    "@types/google-apps-script": "^1.0.98",
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.6",
    "papaparse": "^5.5.3",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
